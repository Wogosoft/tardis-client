# Wogo Tardis Deno Client

This is the tardis client for deno. To build, run `deno task build` or `npx @bufbuild/buf generate`

The client is generated in the `gen/` folder, based on the `proto/` folder.
