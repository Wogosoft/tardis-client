# Wogo Tardis Deno Client

This is the tardis client for deno. 

The client is generated in the `gen/` folder, based on the `proto/` folder.

## Building the client

To build: 

1. install deps `yarn install`
2. run build  `yarn build`